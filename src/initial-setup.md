sudo apt install \
         openbox \
         lightdm \
         pavucontrol \
         pamixer \
         playerctl \
         wmctrl \
         xdotool \
         unclutter \
         intel-media-va-driver \
         intel-media-va-driver-shaders \
         vainfo \
         evtest \
         libinput-tools \
         chromium-browser

Openbox = lightweight window manager
LightDM = auto-login GUI
pavucontrol → audio settings GUI
pamixer → volume control from scripts/hotkeys
playerctl → media play/pause/next control
wmctrl → force fullscreen windows
xdotool → send keys/mouse events
unclutter → hide mouse cursor

Hardware acceleration:
VAAPI
VP9/AV1 acceleration
smooth YouTube/Kodi playback